# numislandsleetcode200

What the algorithm does is implement a DFS search, where at each 1, we branch to adjacent 1s in all 4 directions using recursion. We then eliminate them by turning them to 0s so that the island is completely counted for and won't be recounted. 
